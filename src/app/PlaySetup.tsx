import React from 'react';
import { Text } from 'react-native';
import { Center } from '../shared/Center';

interface PlaySetupProps {}

export const PlaySetup: React.FC<PlaySetupProps> = ({}) => {
	return (
		<Center>
			<Text>Play Setup Page</Text>
		</Center>
	);
};
